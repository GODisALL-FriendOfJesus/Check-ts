//tuples are arrays with fixed length and they are supported by type script
//eg
const tuples:[number,string]=[1,'hello']
console.log(tuples);
//👀🐱‍🚀
//if the lenght does not mutch this will read type error🌾
//const tupled:[number,number]=[1];
// there will be an error explaining that there is requiremnts of numbers yet visile one 
//is one